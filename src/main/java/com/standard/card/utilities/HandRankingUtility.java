package com.standard.card.utilities;

import java.util.HashMap;
import java.util.List;

/**
 *  The class does gets individual player hands ratings
 */
public class HandRankingUtility {

    /**
     * The method gets a hash map of players, iterate the map to check if a player hand contains five cards of one rank
     * @param playerHand
     */
    public static void getFiveOfKind(HashMap<String, List<String>> playerHand){

    }

    /**
     * The method gets a hash map of players, iterate the map to check if a player hand contains five cards of sequential rank
     * @param playerHand
     */
    public static void getStraightFlush(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains four cards of one rank and one card of another rank
     * @param playerHand
     */
    public static void getFourOfKind(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains three cards of one rank and two cards of another rank
     * @param playerHand
     */
    public static void getFullHouse(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains five cards all of the same suit
     * @param playerHand
     */
    public static void getFlush(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains five cards of sequential rank
     * @param playerHand
     */
    public static void getStraight(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains three cards of one rank and two cards of two other ranks (the kickers)
     * @param playerHand
     */
    public static void getThreeOfKind(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains two cards of one rank, two cards of another rank and one card of a third rank (the kicker)
     * @param playerHand
     */
    public static void getTwoPair(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand contains two cards of one rank and three cards of three other ranks (the kickers)
     * @param playerHand
     */
    public static void getOnePair(HashMap<String, List<String>> playerHand){

    }

    /**
     *  The method gets a hash map of players, iterate the map to check if a player hand does not fall into any other category
     * @param playerHand
     */
    public static void getHighCard(HashMap<String, List<String>> playerHand){

    }
}


